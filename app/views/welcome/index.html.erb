<%= render 'carousel' %>

<div class="container welcome-titles">
  <h1>NEWS</h1>
</div>

<div class="container-fluid">
  <div class="row">
  <% @posts.each do |post| %>
    <div class="col-lg">
      <div class="card mb-4">
      <img class="card-img-top" src="http://placehold.it/300x200" alt="Card image cap">
        <div class="card-body">
          <h2 class="card-title"><%= post.title %></h2>
          <p class="card-text"><%= post.content %></p>
          <div class="btn btn-primary btn-sm"><%= link_to 'Read More', post, :class => "btn btn-primary" %></div>
        </div>
        <div class="card-footer text-muted">Posted on January 1, 2017 by
        <a href="#">Start Bootstrap</a>
        </div>
      </div>
    </div>
<% end %>
  </div>
</div>

<div class="container welcome-titles">
  <h1>SPOTLIGHT</h1>
</div>

